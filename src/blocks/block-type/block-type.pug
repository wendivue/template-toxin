mixin block-type({ list })
  ul.block-type
    each item in list
      -
        let { theme, title, text } = item;
        let itemClass = '';
        const themeSetting = ['h1', 'h2', 'h3', 'body'];

        if(themeSetting.includes(theme)) itemClass += ' block-type__item_typo_' + theme;

      li.block-type__item(class=itemClass)
        p.block-type__title= title
        p.block-type__text= text
