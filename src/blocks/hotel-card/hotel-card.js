import 'owl.carousel';

$('.js-owl-carousel').owlCarousel({
  nav: true,
  items: 1,
  dots: true,
  navText: [
    '<button class="hotel-card__icon">expand_more</button>',
    '<button class="hotel-card__icon">expand_more</button>',
  ],
});
